import 'package:flutter/material.dart';

class AppColors {
  // O Fundo escuro (quase preto)
  static const Color background = Color(0xFF181A20);

  // O Verde "Finzo" (para botões e saldo positivo)
  static const Color primaryGreen = Color(0xFF1FDE82);

  // Cores secundárias para textos
  static const Color white = Colors.white;
  static const Color grey = Color(0xFF616161);
  static const Color darkGrey = Color(0xFF1F222A); // Para fundos de cards

  // Cores de transação
  static const Color income = Color(0xFF00C853); // Verde Receita
  static const Color expense = Color(0xFFF44336); // Vermelho Despesa
}